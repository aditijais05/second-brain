# Week 1–2: Ingestion pipeline
pymupdf>=1.23.0          # PDF extraction
python-frontmatter>=1.1  # Markdown frontmatter
trafilatura>=1.8.0       # Web article extraction

# Testing
pytest>=8.0
pytest-cov>=4.0

# Week 3: Chunking + embeddings
tiktoken>=0.7
qdrant-client>=1.9       # Vector store (no fastembed extra — avoids Rust compile on Render)
openai>=1.30             # Optional: OpenAI embeddings (needs OPENAI_API_KEY)

# Week 4: Hybrid search
rank-bm25>=0.2.2         # BM25 keyword index

# Week 5: API
google-genai>=1.0        # Gemini API — generation + embeddings (text-embedding-004)

# Week 6: UI
streamlit>=1.35          # Web UI

# Week 7: Evaluation
ragas>=0.1.0             # RAG evaluation metrics